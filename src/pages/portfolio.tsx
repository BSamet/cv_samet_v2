import React from 'react';
import Works from "../common/components/elements/works/works";

const Portfolio = () => {
    return (
        <section>
            <Works/>
        </section>
    );
};

export default Portfolio;
